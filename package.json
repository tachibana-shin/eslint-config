{
  "name": "@tachibana-shin/eslint-config",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "release": "pnpm lint && pnpm test && bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "test": "pnpm -r --stream run test"
  },
  "devDependencies": {
    "bumpp": "^7.1.1"
  },
  "engines": {
    "node": ">=10"
  }
}
